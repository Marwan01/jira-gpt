"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toProductContext = void 0;
const DEFAULT_RUNTIME_CONTEXT = {};
const toProductContext = (context, runtimeContext) => {
    const { principal, installContext, workspaceId, license } = runtimeContext || DEFAULT_RUNTIME_CONTEXT;
    const { cloudId, contentId, localId, spaceKey, isConfig, platformContext, extensionContext, moduleKey, environmentId, environmentType, accountType } = context;
    return {
        accountId: principal && principal.accountId,
        accountType,
        cloudId,
        contentId,
        localId,
        spaceKey,
        installContext,
        platformContext,
        isConfig,
        extensionContext,
        license,
        moduleKey,
        environmentId,
        environmentType,
        workspaceId
    };
};
exports.toProductContext = toProductContext;
